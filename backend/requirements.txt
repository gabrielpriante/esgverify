# ESGVerify backend dependencies
# Pin major versions for reproducibility; allow patch updates

# --- Web framework ---
fastapi==0.111.*
uvicorn[standard]==0.30.*
python-multipart==0.0.*         # File upload support

# --- Data validation ---
pydantic==2.7.*
pydantic-settings==2.3.*        # Environment-based config

# --- LLM inference ---
ollama==0.2.*                   # Ollama Python client

# --- NLP / ML ---
transformers==4.41.*            # ClimateBERT and Hugging Face models
torch==2.3.*                    # PyTorch backend for transformers
sentence-transformers==3.0.*    # Embeddings for semantic search

# --- Vector database ---
chromadb==0.5.*                 # Local vector store for evidence retrieval

# --- Document parsing ---
pymupdf==1.24.*                 # PDF text extraction (imported as fitz)
python-docx==1.1.*              # DOCX parsing

# --- Utilities ---
pyyaml==6.0.*                   # Config file parsing
httpx==0.27.*                   # Async HTTP client
tenacity==8.3.*                 # Retry logic for LLM calls
structlog==24.2.*               # Structured logging

# --- Dev / testing ---
pytest==8.2.*
pytest-asyncio==0.23.*
httpx                           # Also used as AsyncClient in tests
